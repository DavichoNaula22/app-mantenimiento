import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.appmantenimiento',
  appName: 'app-mantenimiento',
  webDir: 'dist/myapp/browser'
};

export default config;
